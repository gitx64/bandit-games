If the bashrc is corrupted on server or, if you dont want to interactively open the server for just cat a file's values then:

ssh <username@IP> -p <PORT> "cat <filename>" or any command you want to run in quotes.
