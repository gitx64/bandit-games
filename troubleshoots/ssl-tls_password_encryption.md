If want to get a password which is secured by ssl/tls certifications in server. The command is similar looking and functioning to netcat

openssl s_client -connect <IP>:<PORT> then the password when it goes to new line
